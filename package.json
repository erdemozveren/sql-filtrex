{
  "name": "sql-filtrex",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run build && node test/index.js",
    "build": "node build.js",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "author": "erdemozveren",
  "license": "MIT",
  "description": "SQL-like expression parser to generate Where Expression for end-users",
  "dependencies": {
    "jison": "^0.4.18"
  },
  "devDependencies": {
    "sql-bricks": "^3.0.1"
  }
}
